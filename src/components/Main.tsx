
export const Main = ({ children }: { children: React.ReactNode }) => {
    return (
        <main className=" w-full lg:w-[1200px] mx-auto px-5 lg:px-0">
            {children}
        </main>
    );
 }